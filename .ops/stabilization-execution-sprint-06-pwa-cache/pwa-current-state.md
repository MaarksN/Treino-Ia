# PWA Current State - Sprint 06

| Area | Arquivo | Estado atual | Risco | Acao nesta sprint |
|---|---|---|---|---|
| Service worker registration | `src/main.tsx`, `src/utils/pwaUtils.ts` | `registerSW()` e chamado no boot e registra `/sw.js` quando `navigator.serviceWorker` existe. | Browser automation pode nao expor `navigator.serviceWorker`, impedindo prova direta de registro. | Preview/browser smoke tentou inspecao real; bloqueio documentado. |
| Cache bypass | `public/sw.js` | `shouldBypassCache()` envia requests com `Authorization` e caminhos same-origin `/api/*` para `networkOnly()`. | Drift entre SW real e policy TypeScript poderia reabrir cache indevido. | Teste estatico novo cobre bypass e ausencia de escrita em CacheStorage no `networkOnly()`. |
| Cache policy helper | `src/services/pwa/cachePolicy.ts` | `GET` sem auth usa `cache-first` para assets/app shell; navegacao, dev assets e externo usam `network-first`; `/api/*`, auth e non-GET usam `network-only`. | Helper poderia divergir do SW real ou nao cobrir variacoes de header. | Testes ampliados para header `authorization` lowercase via `Headers` e non-GET. |
| CacheStorage | `public/sw.js` | Usa `treino-app-shell-v1` para app shell/assets e `treino-app-data-v1` para `networkFirst()` sem Authorization. | Inspecao browser direta de entradas de CacheStorage ficou bloqueada; novos endpoints sensiveis fora de `/api/*` exigem cuidado. | Browser tentou `window.caches`; teste estatico garante que `/api/*` e auth nao escrevem cache. |
| Static assets | `public/sw.js`, `dist/assets/*` | Assets same-origin podem usar `cacheFirst()`; build gerou chunks versionados em `/assets`. | Quebra de asset cache afetaria boot offline/app shell. | Browser `pageAssets` observou scripts, stylesheet e fonts carregados no preview. |
| `index.html` | `index.html`, `src/services/pwa/cachePolicy.ts` | Manifest declarado; `index.html` segue politica app-shell/cache-first no helper e navegacao usa network-first no SW. | Offline completo de navegacao nao provado por browser por falta de SW/CacheStorage na automacao. | Mantido comportamento existente; documentado como parcial. |
| Offline fallback | `public/offline.html`, `public/sw.js` | Fallback HTML estatico sem payload de usuario; fallback API retorna JSON 503 com `cache-control: no-store`. | Smoke offline real nao executado; garantia fica por static review/teste. | Teste estatico novo cobre marcadores sensiveis e fallback API no-store. |
| Vercel/API routes | `vercel.json`, `api/**` | Rewrites/crons usam `/api/*`; headers de API variam por Authorization no backend. | Preview local nao simula Vercel Functions reais. | Validacao limitada a SW/policy/static review; OAuth/Billing/API real seguem fora do escopo. |
